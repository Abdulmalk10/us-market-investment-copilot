# Validation checklist

- Plugin manifest passes the bundled plugin validator.
- Every skill passes the bundled skill validator.
- Opportunity scoring rejects missing or out-of-range factors.
- Position sizing rejects invalid values and returns deterministic shares.
- Live market claims require current sources and timestamps.
- Recommendations include scenarios, invalidation, horizon, risks, and limitations.
